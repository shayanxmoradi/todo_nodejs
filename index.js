let name = "shayan"
for (let i = 0; i < 10; i++) {

    console.log('Thank you '+name+' for using WebStorm 💙')
}
function randomInt(min, max) {
    return Math.floor(Math.random() * (max - min)) + min
}
console.log(randomInt(5))


